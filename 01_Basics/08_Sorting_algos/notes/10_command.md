Can you do a **quick sort dry run**? But I don’t want a normal dry run.

I need you to **track all the corresponding variables throughout the recursion**. Also, during **backtracking**, show **which variables are being used and how their values change**.

For **every recursive call**, create a separate **box**, and inside that box, show all the relevant elements/variables for that specific call.

So, I want the dry run to be presented **recursion call by recursion call**, including the **backtracking process**, rather than just showing the final partitioning steps.

You can use **any input**.